{
  "name": "rate-limiter-test",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run lint && npm test"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.11"
  }
}